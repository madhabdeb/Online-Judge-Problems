#include<stdio.h>
int main()
{

    int a,b,c,arr[100];

    scanf("%d",&a)
    for(b=0; b<a; b++)
    {

        scanf("%d",arr[b])
    }
    scanf("%d",& c)
    for(b=0; b<a; b++)

    {
        if (arr[b]==c)
        {

                x++;
            }
        }
        if(x=1)
        {
            printf("found");
        }
            else if
            {
                printf("not found");
            }

            return 0;
        }


    }

}
